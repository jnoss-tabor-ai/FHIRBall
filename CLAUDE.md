# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

FHIRBall is a learning repo for FHIRBuilders (per README.md). As of now the
repository contains only a README and LICENSE — no source code, build
tooling, or tests have been added yet.

There are no build, lint, or test commands to document yet, and no
architecture to describe. When code is added, update this file with:
- the language/framework chosen and how to build, lint, and test it
  (including how to run a single test)
- the high-level structure of the codebase, especially anything that spans
  multiple files/modules and wouldn't be obvious from a directory listing
